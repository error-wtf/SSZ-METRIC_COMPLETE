# Astroquery Fetch manifests

This folder holds generated JSON manifests containing target metadata, data URLs, and download policy rules.
Do not commit actual raw downloaded files (FITS, event files) to Git. Only the manifests are tracked.
